# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/

# Node build output (keep out of git; CI builds it fresh, local deploy needs it present)
# Note: static/ is committed so databricks bundle deploy (which respects .gitignore) picks it up
# The GitHub Actions workflow rebuilds it before deploying anyway

# Databricks CLI state
.databricks/

# Environment files
.env
.env.*
!.env.example
*.local

# macOS
.DS_Store

# IDE
.vscode/
.idea/
